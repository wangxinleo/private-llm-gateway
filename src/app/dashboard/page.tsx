export default function DashboardPage() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        minHeight: "100vh",
        fontFamily: "system-ui, sans-serif",
      }}
    >
      <div style={{ textAlign: "center" }}>
        <h1>Privacy Proxy</h1>
        <p>Dashboard placeholder - coming in a future iteration.</p>
      </div>
    </div>
  );
}
